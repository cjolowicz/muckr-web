// @flow
import React from "react";

import MenuButton from "../MenuButton";
import render from "../../test/render";

describe("MenuButton", () => {
  it("renders", () => {
    render(<MenuButton />);
  });
});
